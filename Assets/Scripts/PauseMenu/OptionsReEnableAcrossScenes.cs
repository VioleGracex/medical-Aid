using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OptionsReEnableAcrossScenes : MonoBehaviour
{
    // Start is called before the first frame update
    public void ReEnableGeneralMenu()
    {
       /*  if(GameObject.FindGameObjectWithTag("Canvas").GetComponent<EnableAChild>() != null)
        {
            GameObject.FindGameObjectWithTag("Canvas").GetComponent<EnableAChild>().EnableMyChild();
        }   */
    }
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
